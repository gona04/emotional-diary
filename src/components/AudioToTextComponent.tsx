// AudioToTextComponent intentionally removed. The project now uses the
// browser Web Speech API directly in `RecorderComponent`. This file is
// left as a placeholder to avoid breaking imports during incremental
// cleanup; you can delete this file entirely if you prefer.

export default null as any;